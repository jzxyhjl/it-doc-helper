# IT学习辅助系统 - 环境变量配置模板
# 
# 使用说明：
# 1. 复制此文件为 .env: cp .env.example .env
# 2. 编辑 .env 文件，填入你的实际配置值
# 3. 注意：.env 文件不会被提交到 Git（已在 .gitignore 中）

# ============================================
# 必需配置（必须填写）
# ============================================

# 数据库配置
POSTGRES_DB=it_doc_helper
POSTGRES_USER=it_doc_helper
POSTGRES_PASSWORD=your_secure_password_here  # ⚠️ 请修改为强密码

# DeepSeek API配置（必填，用于AI功能）
# 获取方式：访问 https://www.deepseek.com/ 注册账号，在API管理页面创建API Key
DEEPSEEK_API_KEY=sk-your-deepseek-api-key-here  # ⚠️ 请填入你的DeepSeek API Key
DEEPSEEK_API_BASE=https://api.deepseek.com

# ============================================
# 可选配置（有默认值，可根据需要修改）
# ============================================

# 向量化服务配置
# 是否使用本地嵌入模型（推荐使用本地模型，无需额外API费用）
USE_LOCAL_EMBEDDING=true
# 本地嵌入模型名称（如果 USE_LOCAL_EMBEDDING=true）
EMBEDDING_MODEL_NAME=sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2
# OpenAI API Key（可选，如果 USE_LOCAL_EMBEDDING=false 时使用）
OPENAI_API_KEY=

# 文件上传配置
UPLOAD_MAX_SIZE=31457280  # 文件大小限制（字节），默认30MB
ALLOWED_EXTENSIONS=pdf,docx,pptx,md,txt  # 允许的文件扩展名（逗号分隔）

# 日志配置
LOG_LEVEL=INFO  # 日志级别：DEBUG, INFO, WARNING, ERROR

# CORS配置（开发环境，生产环境请修改）
# 允许的前端访问来源（逗号分隔）
CORS_ORIGINS=http://localhost,http://localhost:80,http://localhost:3000,http://localhost:5173,http://localhost:5174,http://localhost:5175,http://localhost:5176

# ============================================
# 数据库连接配置（Docker环境自动配置，无需修改）
# ============================================
# DATABASE_URL 和 REDIS_URL 在 Docker 环境下会自动配置
# 如果本地开发不使用 Docker，需要手动配置：
# DATABASE_URL=postgresql://it_doc_helper:your_password@localhost:5432/it_doc_helper
# REDIS_URL=redis://localhost:6379/0
